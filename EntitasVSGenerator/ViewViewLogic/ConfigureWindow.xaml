<UserControl x:Class="EntitasVSGenerator.ConfigureWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Name="MyToolWindow"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <DockPanel HorizontalAlignment="Stretch">
        
        <Label FontSize="20" Foreground="White" Content="Added Triggers" DockPanel.Dock="Top" Margin="10"/>
        <Grid Margin="10,5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition  Width="Auto"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" Margin="0,0,0,0">
                <ListBox x:Name="LstBoxPaths" VerticalContentAlignment="Stretch"/>
            </DockPanel>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Button x:Name="BtnAdd" Content="Add" Click="Add_Click"/>
                <Button x:Name="BtnModify" Content="Modify" Click="Modify_Click"/>
                <Button x:Name="BtnDelete" Content="Delete" Click="Delete_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>