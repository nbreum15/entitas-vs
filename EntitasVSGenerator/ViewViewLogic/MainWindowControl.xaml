<UserControl x:Class="EntitasVSGenerator.MainWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="250"
             Name="MyToolWindow"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
        <DockPanel>
        <ComboBox x:Name="TabSelect" DockPanel.Dock="Top" SelectedIndex="0" SelectionChanged="TabSelectionChanged">
                <ComboBoxItem Content="Overview"/>
                <ComboBoxItem Content="Configure"/>
            </ComboBox>
            <DockPanel x:Name="ViewContainer">
            </DockPanel>
    </DockPanel>
</UserControl>
