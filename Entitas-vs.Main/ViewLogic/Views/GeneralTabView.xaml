<UserControl x:Class="EntitasVSGenerator.ViewLogic.Views.GeneralTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EntitasVSGenerator.ViewLogic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Top">
        <TextBlock Text="Generator Path:"/>
        <DockPanel>
            <Button Content="..." MinWidth="50" DockPanel.Dock="Right" Command="{Binding ChangeGeneratorPathCommand}"/>
            <TextBox Text="{Binding GeneratorPath}" Name="PathBox" DockPanel.Dock="Left" />
        </DockPanel>
        <Separator/>
        <TextBlock Text="Add new project:"/>
        <DockPanel>
            <Button Content="Add" MinWidth="50" DockPanel.Dock="Right" Command="{Binding AddProjectCommand}"/>
            <ComboBox ItemsSource="{Binding ProjectNames}" SelectedValue="{Binding SelectedProjectName}" DockPanel.Dock="Right"></ComboBox>
        </DockPanel>
        <Separator/>
    </StackPanel>
</UserControl>
