<UserControl x:Class="EntitasVSGenerator.ViewLogic.Views.ProjectTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EntitasVSGenerator.ViewLogic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Top">
        <TextBlock Text="Triggers:"/>
        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
            <Button Content="Add" Margin="0,0,5,0" Command="{Binding AddTriggerCommand}"/>
            <Button Content="Delete" Command="{Binding DeleteTriggerCommand}"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Triggers}" />
    </StackPanel>
</UserControl>
